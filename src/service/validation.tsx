import React from 'react'
import productData from '../types'

function FilterValidation({data}: {data: productData[]}) {
  return (
    <div>FilterValidation</div>
  )
}

export default FilterValidation